local basic = {
	buildElement = require(script.ElementHandler),
	render = require(script.RenderHandler),
	animate = require(script.AnimateHandler),
	
	-- Beta Features (Buggy)
}

return basic

